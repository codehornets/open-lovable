# Don't re-run `pnpm install` before every `pnpm <script>` — the frozen install is
# authoritative here, and the pre-run check was hard-failing the build wrapper.
# (Native build scripts are allow-listed via pnpm.onlyBuiltDependencies in package.json.)
verify-deps-before-run=false
